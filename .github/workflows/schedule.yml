name: name for sachdule
runner-name: runner name for schedule

on:
  schedule:
    - cron: "*/1 * * * *"

jobs:
  name: my_first_job
    runs-on: ubuntu-latest
    steps:
      - name: echo time
        run: echo "the current time is $(date)"
